export default class SwaggerDefinitions {
  definitions = {
    CommonApiFailed: {
      properties: {
        code: {
          type: "integer",
        },
        message: {
          type: "string",
        },
      },
    }
  }
}
